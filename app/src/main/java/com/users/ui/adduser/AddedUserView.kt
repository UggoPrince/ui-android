package com.users.ui.adduser

/**
 * User details post authentication that is exposed to the UI
 */
data class AddedUserView(
    val displayName: String
    //... other data fields that may be accessible to the UI
)